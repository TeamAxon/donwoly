# Config

Qdrant와 임베딩 관련 설정을 모아두는 폴더입니다.

- Collection 이름: `first_month_guide`
- Embedding 모델: `text-embedding-3-small`
- Vector dimension: `1536`
- Distance metric: `Cosine`

나중에 백엔드와 Qdrant 적재 스크립트가 같은 설정을 쓰도록 이 폴더에서 값을 관리합니다.
